# Processed Data

This folder contains deterministic outputs from cleaning and feature engineering steps.

Current production-grade starter output:
- `production_feature_matrix.csv`

These files should be reproducible from raw inputs via the C/C++ pipeline implementation in `src/pipeline/`.
