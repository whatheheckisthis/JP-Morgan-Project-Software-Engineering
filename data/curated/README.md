# Curated Data

Use this folder for stable, analysis-ready datasets consumed by modeling and reporting.

Current production-grade starter output:
- `production_risk_profiles.csv`

Curated artifacts should include clear provenance back to `data/processed/` outputs.
